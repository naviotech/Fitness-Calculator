export type Alert = {
  message: string,
  error: boolean
}

export type Food = {
  id:string,
  tipo: string,
  gramos: number,
  calorias: number
}

export type Activity = {
  id:string,
  tipo: string,
  duracion:string,
  calorias: number
}